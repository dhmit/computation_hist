{% extends "base.jinja2" %}

{% block title %} {{person_obj.first}} {{ person_obj.last }} {% endblock %}

{% block self %}
    <h1>
        {{person_obj.first}} {{person_obj.last}}
    </h1>
{% endblock %}

{% block table %}

    {% if document_written_objs %}
        <h3>{{ person_obj.first }} {{ person_obj.last }} wrote:</h3>
        <br>
        <table class="table">
            <thead class="thead-dark">
            <tr>
                <th scope="col">#</th>
                <th scope="col">Document Name</th>

            </tr>
            </thead>
            <tbody>
            {% for item in document_written_objs %}
                <tr>
                    <th scope="row">{{ loop.index }} </th>

                    <td><a href="/dj_comp_hist/doc/{{ item.pk }}">{{ item }}</a></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% endif %}

    {% if document_received_objs %}
        <h3>{{ person_obj.first }} {{ person_obj.last }} received:</h3>
        <br>
        <table class="table">
            <thead class="thead-dark">
            <tr>
                <th scope="col">#</th>
                <th scope="col">Document Name</th>

            </tr>
            </thead>
            <tbody>
            {% for item in document_received_objs %}
                <tr>
                    <th scope="row">{{ item.id }} </th>

                    <td><a href="/dj_comp_hist/doc/{{ item.pk }}">{{ item }}</a></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% endif %}

    {% if document_cced_objs %}
        <h3>{{ person_obj.first }} {{ person_obj.last }} was cced on:</h3>
        <br>
        <table class="table">
            <thead class="thead-dark">
            <tr>
                <th scope="col">#</th>
                <th scope="col">Document Name</th>

            </tr>
            </thead>
            <tbody>
            {% for item in document_cced_objs %}
                <tr>
                    <th scope="row">{{ item.id }} </th>

                    <td><a href="/dj_comp_hist/doc/{{ item.pk }}">{{ item }}</a></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% endif %}

{% endblock %}
